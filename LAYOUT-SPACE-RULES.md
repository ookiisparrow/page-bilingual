# 排版 + 空间硬规则（短清单）

> 服务目标：稳、快、清晰省屏、少错、广站。  
> 配合 `DESKTOP-ACCEPTANCE.md` 多站×双视口；1.4.7+ skip≠fail。

---

## A. 硬规则（不过即 bug）

### 1. stack / row
| 规则 | 要求 |
| --- | --- |
| 手机宽（≤600） | **一律 stack**，禁止 row |
| 桌面 | 默认 stack；row 仅短标签且测宽通过 |
| flex/grid 行内宿主 | **禁止** `after()` 抢同行；不译或插到整行容器之后并 `width:100%` 换行 |
| 失败回退 | row 后译文自身多行 → 立刻改 stack |

### 2. 双语 / 仅译文
| 规则 | 要求 |
| --- | --- |
| 双语 | 原文保留；译文在块下（或测宽通过的同行）；**各出现一次** |
| 仅译文 | 中文**只一次**；禁止 `textContent=译文` 同时留可见兄弟 tr |
| 切模式 | 无双显、无残留 `…`、无段间空洞暴涨 |
| pending | **禁止**用 `…` 覆盖正文（尤其仅译文） |

### 3. 行距 / 省屏
| 规则 | 要求 |
| --- | --- |
| 双语间距 | 原文↔译文间隙宜小：约 `0.15～0.35em`，不要大分割线+厚 padding |
| 仅译文 | 行高≈宿主；无额外 margin/border |
| 字号/颜色 | 译文 ≤ 原文字号；颜色略淡即可，勿再降一号又加大行距 |
| 屏效 | 同等内容，双语总高度膨胀相对「仅原文」建议 **< 1.7×**（经验阈值，目测即可） |

### 4. 标题
| 规则 | 要求 |
| --- | --- |
| 折叠/工具条内标题 | **默认不译**（维基 ▾ 行、带 chevron 的 flex 头） |
| 会译的标题 | 译文 **下方整句**；禁止行内左右硬挤断字 |
| 大标题字号 | 译文始终比原文 **小两号**（约 −4pt；适用 h1/h2 或 ≥18px 的 heading） |
| 竖排 | 任何标题出现字母/汉字竖排 = 必修 |

### 5. 导航与 Chrome 克制
| 规则 | 要求 |
| --- | --- |
| 不译 | nav/header/footer、按钮、芯片行、相对时间、文件列表元数据、用户名 |
| SERP 芯片 | 布局≈原站；可不译 |
| 弹出菜单 | 可 Text 替换；不 `after` 进横向 flex 项 |
| 原则 | **正文像书，Chrome 像没装插件** |

---

## B. 会拖慢的点（提速时优先砍）

| 优先级 | 拖慢点 | 建议 |
| --- | --- | --- |
| P0 | 热/冷队列并发抢额度 | **热完再冷**（1.4.7 方向）；冷队列可降并发 |
| P0 | 批太大（长段×24） | 热批 6～8；冷批再放大 |
| P1 | 开译全页 DOM 扫描+样式计算 | collect 可分片；fragile 检测缓存 computedStyle |
| P1 | 每段多次 `getBoundingClientRect` / 测宽探针 | 批内复用视口盒；手机跳过测宽 |
| P1 | 屏外仍高并发打 API | 冷队列 worker≤2；远离视口可暂停 |
| P2 | MO 触发过勤补译 | 保持防抖；只译 popup/新增块 |
| P2 | 失败重试风暴 | 自动重试限次；用户点「重试」再打 |

**提速别碰**：为快而重新 append 进 flex、或恢复全页 `…` 占位——会换回布局事故。

---

## C. 和验收矩阵的挂钩（一眼）

| 规则块 | 主要覆盖 |
| --- | --- |
| A1–A2、pending | G1、G3；维基+新闻 |
| A4–A5 | G2、G4；维基折叠、GitHub 元数据、Google 芯片、菜单 |
| A3 | 全省站目测「是否太疏」 |
| B | 长文耗时；冷队列策略由幕僚长侧跑 |

